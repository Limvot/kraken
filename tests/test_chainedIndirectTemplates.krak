import vector:*;
import simple_print:*;

fun test<T>(a: vector<T>): T {
    return a.at(0);
}

fun main(): int {
    var a.construct(): vector<int>;
    a.addEnd(2);
    println(test<int>(a));
    return 0;
}


