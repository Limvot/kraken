import io:*

fun main(): int {
    io::println("Spam");
    var x: int = 4;
    x += 3;
    x++;
    var y :int = 6;
    var z :int = x + y;
    var q :int = z+z;
    var q2:int = z*3;
    var q3:int = y + y;
    io::println(q3);
    io::println(z);
    io::println(q);
    for (var i:int = 0; i < 20; i++;) z++;
    if (z > 20) io::println("We'll find out.");
    io::println(z);
    var boolean = false
    if ( z > 20 && !boolean)
        io::println("woo not")
    io::println('b' == 'a')
    println(3|5)
    println(3^5)
    println(3&5)
}
