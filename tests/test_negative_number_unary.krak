import io:*

|int| main() {
    |int| a = -1
    println(a)
    println(-a)
    println(+a); // this is still -1! (as C has it, anyway) (darn comment/semicolon interaction)

    |int| b = 7
    println(b)
    println(-b)

    return 0;
}
